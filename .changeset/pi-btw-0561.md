---
"@herbertgao/pi-extensions": patch
---

Advance `@narumitw/pi-btw` companion pin to `0.56.1`.

- `@narumitw/pi-btw@0.56.1`: defer Ctrl+C terminal restoration until input dispatch finishes so Windows fullscreen sessions redraw and scroll correctly (patch fix; no API or behavior change on non-Windows platforms).
