---
"@herbertgao/pi-extensions": patch
---

Advance bundled companion pins: `@czottmann/pi-automode` to 1.16.0 (bounded existing-file authorization, OpenCode session-header fix, missing project-trust API fix) and `pi-lens` to 4.1.4 (test-runner failure delivery, shared config core, LSP registry consolidation, and agent worktree hygiene).
