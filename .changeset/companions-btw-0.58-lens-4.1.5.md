---
"@herbertgao/pi-extensions": patch
---

Advance bundled companion pins: `@narumitw/pi-btw` to `0.58.0` (adds configurable BTW-only exit, thinking-cycle, and bring-to-main keybindings in `/btw` → Settings) and `pi-lens` to `4.1.5` (internal bug fixes including LSP lifecycle improvements and managed npm deduplication).
